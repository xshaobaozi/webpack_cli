const 
    PATH = require('path');

module.exports = {
    input: PATH.resolve(__dirname, '../../src'),
    output: PATH.resolve(__dirname, '../../dist'),
    root: PATH.resolve(__dirname, '../..'),
}
